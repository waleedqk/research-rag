# Copy to .env and adjust values for your environment.
APP_ENV=dev
# uv will create and reuse the virtual environment at this path
UV_PROJECT_ENVIRONMENT=.venv/uv

# Language model configuration
LLM_PROVIDER=local
LLM_MODEL=llama3
OPENAI_API_KEY=sk-...
OLLAMA_HOST=http://localhost:11434

DATA_SOURCE_DIR=./data/source
DATA_DERIVED_DIR=./data/derived
OUTPUT_DIRECTORY=./data/derived/relevance
